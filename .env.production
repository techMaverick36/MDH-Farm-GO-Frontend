# Used automatically by `vite build` (Vercel production builds included).
# Local development still uses the localhost fallback in src/lib/env.ts.
VITE_API_BASE_URL=https://mdh-farm-go-backend.onrender.com/api/v1
